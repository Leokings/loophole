[pytest]
testpaths = tests/direct
markers =
    integration: multi-validator contract tests against GLSim or a GenLayer network
    semantic: opt-in tests requiring a real GenLayer inference provider
